Начну с самого простого и элементарного.
Аннотация типов стоит уже в следующих простых методах, объявляя типы переменных и возвращаемое значение

def get_len(string: str) -> int:
  return len(string)

def is_comfort_temperature(temperature: int) -> bool:
  return temperature > 22
